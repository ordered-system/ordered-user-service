name: CI

on:
  push:
    branches: [ '**' ]
  pull_request:
    branches: [ main ]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Checkout ordered-commons
        uses: actions/checkout@v4
        with:
          repository: ordered-system/ordered-commons
          ref: main
          path: ordered-commons

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'

      - name: Install ordered-commons into local Maven repo
        run: mvn --batch-mode --no-transfer-progress -DskipTests install
        working-directory: ordered-commons

      - name: Check code formatting
        run: mvn --batch-mode --no-transfer-progress spotless:check

  unit-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Checkout ordered-commons
        uses: actions/checkout@v4
        with:
          repository: ordered-system/ordered-commons
          ref: main
          path: ordered-commons

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'

      - name: Install ordered-commons into local Maven repo
        run: mvn --batch-mode --no-transfer-progress -DskipTests install
        working-directory: ordered-commons

      - name: Run unit tests
        run: mvn --batch-mode --no-transfer-progress test

  integration-tests:
    if: github.ref == 'refs/heads/main' || github.base_ref == 'main'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Checkout ordered-commons
        uses: actions/checkout@v4
        with:
          repository: ordered-system/ordered-commons
          ref: main
          path: ordered-commons

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'

      - name: Install ordered-commons into local Maven repo
        run: mvn --batch-mode --no-transfer-progress -DskipTests install
        working-directory: ordered-commons

      - name: Run full verification (unit + integration tests)
        run: mvn --batch-mode --no-transfer-progress verify
